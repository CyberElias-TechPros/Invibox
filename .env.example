# Frontend
VITE_API_BASE=/api/v1

# Cloudflare Worker variables (configure with Wrangler / Cloudflare dashboard)
APP_ENV=development
APP_ORIGIN=http://localhost:5173
DEMO_MODE=true

# Secrets — set with `wrangler secret put`, never commit values
# SESSION_PEPPER=
# RESEND_API_KEY=
# WHATSAPP_ACCESS_TOKEN=
# PAYSTACK_SECRET_KEY=
