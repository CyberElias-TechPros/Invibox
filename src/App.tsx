import { useEffect, useMemo, useState } from 'react';
import { AnimatePresence, motion } from 'framer-motion';
import {
  LayoutDashboard, Sparkles, Users, CalendarDays, Armchair, MessageCircle, ScanLine, Images,
  WalletCards, Handshake, ChartNoAxesCombined, Settings, Search, Bell, Plus, ChevronDown,
  ArrowUpRight, MoreHorizontal, Eye, Send, UserPlus, Clock3, MapPin, Check, X, Copy, QrCode,
  Utensils, Download, Filter, GripVertical, Globe2, Smartphone, Monitor, WandSparkles, Lock,
  Music2, Gift, CircleHelp, Camera, Upload, CheckCircle2, Circle, Megaphone, Mail, Phone,
  TrendingUp, CalendarPlus, ExternalLink, Menu, Heart, Navigation, Share2, Pencil, Trash2, ChevronRight,
  Languages, ShieldCheck, Zap, CloudSun, TicketCheck, Bus, Hotel, BookHeart, Store, Bot, Save
} from 'lucide-react';
import type { PageKey, Guest, GuestStatus } from './types';
import { budget } from './data';
import { EventStoreProvider, useEventStore } from './useStore';
import { api } from './api';
import Marketing from './Marketing';

const nav:[PageKey,string,any][]=[
 ['overview','Overview',LayoutDashboard],['experience','Experience',Sparkles],['guests','Guests',Users],['schedule','Schedule',CalendarDays],
 ['seating','Seating',Armchair],['messages','Messages',MessageCircle],['checkin','Check-in',ScanLine],['memories','Memories',Images],
 ['budget','Budget',WalletCards],['vendors','Vendors',Handshake],['analytics','Analytics',ChartNoAxesCombined],['settings','Settings',Settings]
];
const fmt=(n:number)=>new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN',maximumFractionDigits:0}).format(n);

function Toast({text}:{text:string}){return <motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} exit={{opacity:0,y:15}} className="toast"><CheckCircle2 size={17}/>{text}</motion.div>}
function Modal({children,onClose,wide=false}:{children:React.ReactNode,onClose:()=>void,wide?:boolean}){return <motion.div className="modal-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onMouseDown={onClose}><motion.div className={'modal '+(wide?'modal-wide':'')} initial={{opacity:0,scale:.97,y:12}} animate={{opacity:1,scale:1,y:0}} exit={{opacity:0,scale:.98}} onMouseDown={e=>e.stopPropagation()}>{children}</motion.div></motion.div>}
function Status({value}:{value:GuestStatus}){return <span className={'status '+value.toLowerCase()}><i/>{value}</span>}
function Avatar({initials,muted=false}:{initials:string,muted?:boolean}){return <span className={'avatar '+(muted?'muted':'')}>{initials}</span>}

function Sidebar({page,setPage,mobile,setMobile}:{page:PageKey,setPage:(p:PageKey)=>void,mobile:boolean,setMobile:(v:boolean)=>void}){const {event,events,switchEvent,logout}=useEventStore();
 return <aside className={'sidebar '+(mobile?'open':'')}>
  <div className="brand"><span className="brandmark">i</span><b>invibox</b><button className="mobile-close" onClick={()=>setMobile(false)}><X/></button></div>
  <label className="event-select"><div className="event-thumb">{event?.title?.split(/\s+/).filter(x=>x!=='&').slice(0,2).map(x=>x[0]).join('')||'EV'}</div><div><small>Current event</small><select aria-label="Current event" value={event?.id||''} onChange={e=>switchEvent(e.target.value)}>{events.map(item=><option key={item.id} value={item.id}>{item.title}</option>)}</select></div><ChevronDown size={16}/></label>
  <nav>{nav.map(([key,label,Icon])=><button key={key} className={page===key?'active':''} onClick={()=>{setPage(key);setMobile(false)}}><Icon size={18}/><span>{label}</span>{key==='guests'&&<em>214</em>}</button>)}</nav>
  <div className="sidebar-bottom"><a className="help-link" href="mailto:help@invibox.app"><CircleHelp size={18}/>Help centre</a><div className="profile"><Avatar initials="AO"/><div><strong>Amaka Okafor</strong><small>Event owner</small></div><button className="plain" title="Sign out" aria-label="Sign out" onClick={logout}><ExternalLink size={16}/></button></div></div>
 </aside>
}

function Topbar({page,onMobile,onNew,onPreview}:{page:PageKey,onMobile:()=>void,onNew:()=>void,onPreview:()=>void}){
 const title=nav.find(n=>n[0]===page)?.[1]; const {online,loading,error,event}=useEventStore();const date=event?.starts_at?new Intl.DateTimeFormat('en-NG',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date(event.starts_at)):'Date to be confirmed';
 return <header className="topbar"><button className="menu-btn" onClick={onMobile}><Menu/></button><div><h1>{title}</h1><p>{date} <span>•</span> {event?.location||'Location to be confirmed'}</p></div><div className="top-actions"><span className={'sync-state '+(online?'online':'offline')} title={error||'Cloudflare backend connected'}><i/>{loading?'Connecting':online?'Synced':'Offline cache'}</span><button className="btn ghost preview-top" onClick={onPreview}><Eye size={17}/> View invite</button><button className="btn primary" onClick={onNew}><Plus size={17}/> New event</button></div></header>
}

function Overview({setPage,notify}:{setPage:(p:PageKey)=>void,notify:(s:string)=>void}){
 const {guests,event}=useEventStore(); const attending=guests.filter(g=>g.status==='Attending').reduce((a,g)=>a+g.party,0); const pending=guests.filter(g=>g.status==='Pending').reduce((a,g)=>a+g.party,0);const declined=guests.filter(g=>g.status==='Declined').reduce((a,g)=>a+g.party,0);const invited=guests.reduce((a,g)=>a+g.party,0);const days=Math.max(0,Math.ceil(((event?.starts_at?new Date(event.starts_at).getTime():Date.now())-Date.now())/86400000));
 return <div className="page overview-page">
  <section className="welcome"><div><span className="eyebrow">GOOD AFTERNOON, AMAKA</span><h2>Your celebration is coming<br/><i>beautifully together.</i></h2><p>{days} days to go. Here’s what’s happening with {event?.title||'your event'}.</p></div><div className="countdown"><div><strong>{String(days).padStart(3,'0')}</strong><small>DAYS</small></div><span>:</span><div><strong>14</strong><small>HOURS</small></div><span>:</span><div><strong>22</strong><small>MIN</small></div></div></section>
  <section className="metric-grid">
   <Metric title="Total invited" value={String(invited)} detail={`${guests.length} guest records`} icon={Users} tone="sage"/>
   <Metric title="Attending" value={String(attending)} detail={`${invited?Math.round(attending/invited*100):0}% response rate`} icon={CheckCircle2} tone="green"/>
   <Metric title="Awaiting reply" value={String(pending)} detail={pending?'Ready for a gentle reminder':'Everyone has replied'} icon={Clock3} tone="amber" action={pending?'Send reminder':undefined} onAction={()=>{setPage('messages');notify(`Prepare a reminder for ${pending} awaiting guests`)}}/>
   <Metric title="Declined" value={String(declined)} detail={`${invited?Math.round(declined/invited*100):0}% of invitees`} icon={X} tone="rose"/>
  </section>
  <div className="overview-grid">
   <section className="card activity-card"><CardHead title="Guest activity" sub="Responses over the last 14 days" action="View guests" onAction={()=>setPage('guests')}/><ActivityChart/><div className="chart-legend"><span><i className="green-dot"/>Attending <b>147</b></span><span><i className="amber-dot"/>Pending <b>51</b></span><span><i className="rose-dot"/>Declined <b>16</b></span></div></section>
   <section className="card actions-card"><CardHead title="Quick actions" sub="Keep things moving"/><div className="quick-grid"><Quick icon={UserPlus} label="Add guests" onClick={()=>setPage('guests')}/><Quick icon={Send} label="Send update" onClick={()=>setPage('messages')}/><Quick icon={Eye} label="Preview invite" onClick={()=>window.open(`/invite/${event?.slug||'amaka-chidi'}`,'_blank')}/><Quick icon={QrCode} label="Check-in QR" onClick={()=>setPage('checkin')}/><Quick icon={Armchair} label="Plan seating" onClick={()=>setPage('seating')}/><Quick icon={Download} label="Export list" onClick={()=>notify('Guest list exported')}/></div></section>
  </div>
  <div className="overview-grid bottom-grid"><section className="card timeline-card"><CardHead title="Coming up" sub="Your planning timeline" action="Full schedule" onAction={()=>setPage('schedule')}/><Timeline setPage={setPage}/></section><section className="card invite-card"><div className="invite-mini"><img src="/wedding-hero.jpg"/><div className="invite-mini-copy"><span>{event?.event_type?.toUpperCase()||'YOUR EVENT'}</span><strong>{event?.title||'Untitled event'}</strong><small>{event?.starts_at?.slice(0,10).split('-').reverse().join(' · ')||'DATE TO COME'}</small></div></div><div className="invite-info"><div><span className="live-dot"/>{event?.lifecycle||'Draft'}</div><strong>{event?.slug||'event'}.invibox.app</strong><div className="inline-actions"><button onClick={()=>{navigator.clipboard?.writeText(`${location.origin}/invite/${event?.slug||'event'}`);notify('Invite link copied')}}><Copy size={16}/>Copy link</button><button onClick={()=>window.open(`/invite/${event?.slug||'amaka-chidi'}`,'_blank')}><ExternalLink size={16}/></button></div></div></section></div>
 </div>
}
function Metric({title,value,detail,icon:Icon,tone,trend,action,onAction}:{title:string,value:string,detail:string,icon:any,tone:string,trend?:string,action?:string,onAction?:()=>void}){return <div className="metric card"><div className={'metric-icon '+tone}><Icon size={19}/></div><div className="metric-title">{title}<MoreHorizontal size={17}/></div><strong>{value}</strong><div className="metric-foot"><span>{detail}</span>{trend&&<em><TrendingUp size={12}/>{trend}</em>}{action&&<button onClick={onAction}>{action}<ArrowUpRight size={12}/></button>}</div></div>}
function CardHead({title,sub,action,onAction}:{title:string,sub:string,action?:string,onAction?:()=>void}){return <div className="card-head"><div><h3>{title}</h3><p>{sub}</p></div>{action&&<button onClick={onAction}>{action}<ChevronRight size={14}/></button>}</div>}
function Quick({icon:Icon,label,onClick}:{icon:any,label:string,onClick:()=>void}){return <button className="quick" onClick={onClick}><span><Icon size={19}/></span>{label}<ChevronRight size={14}/></button>}
function ActivityChart(){return <div className="chart"><div className="ylabels"><span>18</span><span>12</span><span>6</span><span>0</span></div><div className="chart-area"><svg viewBox="0 0 560 145" preserveAspectRatio="none"><defs><linearGradient id="area" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#727d6a" stopOpacity=".28"/><stop offset="1" stopColor="#727d6a" stopOpacity="0"/></linearGradient></defs><path d="M0 120 C35 112 48 90 82 98 S130 108 157 69 S205 72 240 80 S294 58 321 67 S365 34 401 50 S447 68 475 37 S520 18 560 28 L560 145 L0 145Z" fill="url(#area)"/><path d="M0 120 C35 112 48 90 82 98 S130 108 157 69 S205 72 240 80 S294 58 321 67 S365 34 401 50 S447 68 475 37 S520 18 560 28" fill="none" stroke="#727d6a" strokeWidth="2.5"/></svg><div className="xlabels"><span>Jun 2</span><span>Jun 5</span><span>Jun 8</span><span>Jun 11</span><span>Today</span></div></div></div>}
function Timeline({setPage}:{setPage:(p:PageKey)=>void}){return <div className="timeline"><div><i className="done"><Check size={12}/></i><span><b>Invitation published</b><small>2 June 2026</small></span><em>Done</em></div><div><i/><span><b>First RSVP reminder</b><small>In 7 days</small></span><button onClick={()=>setPage('messages')}>Review</button></div><div><i/><span><b>Finalize guest list</b><small>1 November 2026</small></span><button onClick={()=>setPage('guests')}>Open</button></div><div><i/><span><b>Big day</b><small>19 December 2026</small></span><Heart size={15}/></div></div>}

function GuestsPage({notify}:{notify:(s:string)=>void}){
 const {guests,setGuests,eventId,event,refresh}=useEventStore(); const [search,setSearch]=useState(''); const [filter,setFilter]=useState('All'); const [add,setAdd]=useState(false);
 const shown=guests.filter(g=>(filter==='All'||g.status===filter)&&g.name.toLowerCase().includes(search.toLowerCase()));
 const exportCsv=()=>{const text='Name,Group,Status,Party,Meal,Table\n'+guests.map(g=>[g.name,g.group,g.status,g.party,g.meal,g.table||''].join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text]));a.download='amaka-chidi-guests.csv';a.click();notify('Guest list exported')};
 return <div className="page"><PageIntro title="Guest list" text="Manage invitations, households and responses in one place." actions={<><label className="btn ghost import-btn"><Upload size={16}/>Import CSV<input type="file" accept=".csv,text/csv" onChange={e=>{const file=e.target.files?.[0];if(!file)return;file.text().then(text=>{const lines=text.split(/\r?\n/).filter(Boolean);const headers=lines.shift()?.split(',').map(x=>x.trim().toLowerCase())||[];const imported=lines.map((line,i)=>{const cells=line.split(',').map(x=>x.trim().replace(/^"|"$/g,''));const get=(name:string)=>cells[headers.indexOf(name)]||'';const name=get('name')||get('guest name');if(!name)return null;return {id:Date.now()+i,name,initials:name.split(' ').slice(0,2).map(x=>x[0]).join('').toUpperCase(),group:get('group')||'Imported',status:(['Attending','Pending','Declined'].includes(get('status'))?get('status'):'Pending') as GuestStatus,party:Math.max(1,Number(get('party')||1)),meal:get('meal')||'—',checkedIn:false}}).filter(Boolean) as Guest[];setGuests([...guests,...imported]);notify(`${imported.length} guests imported`)})}}/></label><button className="btn ghost" onClick={exportCsv}><Download size={16}/>Export</button><button className="btn primary" onClick={()=>setAdd(true)}><UserPlus size={16}/>Add guest</button></>}/>
  <div className="summary-strip"><span><b>{guests.length}</b>Total records</span><span><b>{guests.filter(g=>g.status==='Attending').length}</b>Attending</span><span><b>{guests.filter(g=>g.status==='Pending').length}</b>Awaiting reply</span><span><b>{guests.reduce((a,g)=>a+g.party,0)}</b>Seats allocated</span></div>
  <section className="card table-card"><div className="table-toolbar"><div className="searchbox"><Search size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search guests, groups…"/></div><div className="filter-tabs">{['All','Attending','Pending','Declined'].map(x=><button className={filter===x?'active':''} onClick={()=>setFilter(x)} key={x}>{x}</button>)}</div></div>
  <div className="data-table"><div className="tr th"><span>Guest</span><span>Group</span><span>Response</span><span>Party</span><span>Meal preference</span><span>Table</span><span/></div>{shown.map(g=><div className="tr" key={g.id}><span className="guest-name"><Avatar initials={g.initials}/><b>{g.name}</b></span><span>{g.group}</span><span><Status value={g.status}/></span><span>{g.party}</span><span>{g.meal}</span><span>{g.table||'—'}</span><span><button className="plain" onClick={()=>setGuests(guests.filter(x=>x.id!==g.id))}><Trash2 size={15}/></button></span></div>)}</div></section>
  <AnimatePresence>{add&&<AddGuest onClose={()=>setAdd(false)} onAdd={async g=>{const result=await api.addGuest(eventId,g);await refresh();setAdd(false);const link=`${location.origin}/invite/${event?.slug||'event'}?token=${encodeURIComponent(result.token)}`;await navigator.clipboard.writeText(link);notify('Guest added — personalized link copied')}}/>}</AnimatePresence>
 </div>
}
function AddGuest({onClose,onAdd}:{onClose:()=>void,onAdd:(g:Guest)=>Promise<void>}){const [saving,setSaving]=useState(false);const [name,setName]=useState('');const [group,setGroup]=useState('Friends');const [party,setParty]=useState(1);return <Modal onClose={onClose}><div className="modal-head"><div><span className="eyebrow">NEW INVITATION</span><h2>Add a guest</h2><p>Create a personalized invite and reserve their seats.</p></div><button className="icon-btn" onClick={onClose}><X/></button></div><div className="form-grid"><label className="full">Guest or household name<input autoFocus value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. The Balogun Family"/></label><label>Guest group<select value={group} onChange={e=>setGroup(e.target.value)}><option>Friends</option><option>Bride's family</option><option>Groom's family</option><option>VIP</option><option>Colleagues</option></select></label><label>Reserved seats<input type="number" min="1" max="20" value={party} onChange={e=>setParty(+e.target.value)}/></label><label>Email address<input type="email" placeholder="guest@example.com"/></label><label>Phone / WhatsApp<input placeholder="+234…"/></label></div><div className="checkline"><input type="checkbox" defaultChecked/> Send their personalized invitation after saving</div><div className="modal-actions"><button className="btn ghost" onClick={onClose}>Cancel</button><button className="btn primary" disabled={!name||saving} onClick={async()=>{setSaving(true);try{await onAdd({id:Date.now(),name,initials:name.split(' ').slice(0,2).map(s=>s[0]).join('').toUpperCase(),group,status:'Pending',party,meal:'—',checkedIn:false})}finally{setSaving(false)}}}>{saving?'Adding…':'Add guest'} <ArrowUpRight size={15}/></button></div></Modal>}

function Experience({notify}:{notify:(s:string)=>void}){
 const [device,setDevice]=useState('desktop');const [eyebrow,setEyebrow]=useState('Together with our families');const [titleStyle,setTitleStyle]=useState('serif');const [overlay,setOverlay]=useState(54);const [musicEnabled,setMusicEnabled]=useState(false);const [performance,setPerformance]=useState(true); const {sections,setSections,event,eventId}=useEventStore();
 const addSection=()=>setSections([...sections,'New section']);
 return <div className="studio-page"><div className="studio-toolbar"><div><h2>Invitation experience</h2><span className="saved"><Check size={12}/>All changes saved</span></div><div className="device-switch"><button className={device==='mobile'?'active':''} onClick={()=>setDevice('mobile')}><Smartphone size={16}/></button><button className={device==='desktop'?'active':''} onClick={()=>setDevice('desktop')}><Monitor size={16}/></button></div><div><button className="btn ghost" onClick={()=>window.open(`/invite/${event?.slug||'amaka-chidi'}`,'_blank')}><Eye size={16}/>Preview</button><button className="btn primary" onClick={async()=>{try{await api.updateEvent(eventId,{lifecycle:'published'});notify('Experience published successfully')}catch(e){notify(e instanceof Error?e.message:'Could not publish')}}}><Globe2 size={16}/>Publish</button></div></div>
 <div className="studio"><aside className="builder-panel"><div className="panel-tabs"><button className="active">Experience sections</button></div><div className="builder-title"><div><h3>Page sections</h3><p>Drag to reorder your experience</p></div><button onClick={addSection}><Plus/></button></div><div className="section-list">{sections.map((s,i)=><motion.div layout key={i}><GripVertical size={16}/><span className="section-icon">{[Heart,BookHeart,CalendarDays,Clock3,Images,CheckCircle2,Gift,CircleHelp][i%8]({size:15})}</span><b>{s}</b><button className="plain" onClick={()=>setSections(sections.filter((_,n)=>n!==i))}><MoreHorizontal size={16}/></button></motion.div>)}</div><button className="ai-btn" onClick={()=>{const recommended=['Welcome','Event details','Schedule','RSVP'];const missing=recommended.filter(x=>!sections.includes(x));if(missing.length)setSections([...sections,...missing]);notify(missing.length?`Added ${missing.length} recommended sections`:'Your experience includes every essential section')}}><WandSparkles size={18}/><span><b>Smart design check</b><small>Complete essential sections</small></span><ArrowUpRight size={15}/></button></aside>
 <main className="canvas"><div className={'browser-frame '+device}><div className="browser-bar"><i/><i/><i/><span>amaka-chidi.invibox.app</span></div><div className="canvas-invite"><img src="/wedding-hero.jpg"/><div className="shade" style={{opacity:overlay/100}}/><div className="canvas-copy"><span>{eyebrow.toUpperCase()}</span><h1 className={`title-${titleStyle}`}>{event?.title||<>Amaka <i>&</i><br/>Chidi</>}</h1><div className="fine-line"/><p>We joyfully invite you to celebrate<br/>our wedding</p><strong>19 · DECEMBER · 2026</strong><button>OPEN INVITATION <ChevronDown size={15}/></button></div><div className="edit-tag"><Pencil size={12}/>Edit hero</div></div></div></main>
 <aside className="style-panel"><h3>Hero section</h3><label>Composition<select onChange={e=>notify(`${e.target.value} composition selected`)}><option>Editorial overlay</option><option>Split composition</option><option>Classic card</option></select></label><label>Eyebrow text<input value={eyebrow} onChange={e=>setEyebrow(e.target.value)}/></label><label>Title style<div className="seg">{['serif','script','modern'].map(style=><button key={style} className={titleStyle===style?'active':''} onClick={()=>setTitleStyle(style)}>{style[0].toUpperCase()+style.slice(1)}</button>)}</div></label><label>Overlay intensity<input aria-label="Overlay intensity" type="range" value={overlay} onChange={e=>setOverlay(Number(e.target.value))}/></label><label>Motion profile<select onChange={e=>notify(`${e.target.value} motion profile applied`)}><option>Elegant reveal</option><option>Cinematic</option><option>Minimal</option></select></label><div className="tog-row"><span><Music2 size={16}/>Background music</span><button aria-label="Toggle background music" onClick={()=>setMusicEnabled(!musicEnabled)} className={'toggle '+(musicEnabled?'on':'')}><i/></button></div><div className="tog-row"><span><Zap size={16}/>Performance mode</span><button aria-label="Toggle performance mode" onClick={()=>setPerformance(!performance)} className={'toggle '+(performance?'on':'')}><i/></button></div></aside></div></div>
}

function SchedulePage({notify}:{notify:(s:string)=>void}){const {schedule,setSchedule}=useEventStore();const [add,setAdd]=useState(false);return <div className="page"><PageIntro title="Event schedule" text="One celebration, every moment beautifully coordinated." actions={<><button className="btn ghost" onClick={()=>{const items=schedule.map(s=>`BEGIN:VEVENT\r\nDTSTART:20261219T${s.time.replace(':','')}00\r\nSUMMARY:${s.title}\r\nLOCATION:${s.place}\r\nEND:VEVENT`).join('\r\n');const blob=new Blob([`BEGIN:VCALENDAR\r\nVERSION:2.0\r\n${items}\r\nEND:VCALENDAR`],{type:'text/calendar'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='event-schedule.ics';a.click();URL.revokeObjectURL(a.href)}}><CalendarPlus size={16}/>Calendar feed</button><button className="btn primary" onClick={()=>setAdd(true)}><Plus size={16}/>Add moment</button></>}/><div className="schedule-date-label"><CalendarDays size={15}/><span>All event moments</span><b>{schedule.length}</b></div><section className="schedule-list">{schedule.map((s,i)=><motion.div layout className="schedule-row card" key={s.id}><div className="schedule-time"><b>{s.time}</b><span>{i<2?'AM':'PM'}</span></div><div className="schedule-line"><i/></div><div className="schedule-detail"><span className="eyebrow">{s.audience}</span><h3>{s.title}</h3><p><MapPin size={14}/>{s.place}</p></div><div className="schedule-actions"><button className="icon-btn" aria-label={`Delete ${s.title}`} onClick={()=>setSchedule(schedule.filter(x=>x.id!==s.id))}><Trash2 size={16}/></button></div></motion.div>)}</section><AnimatePresence>{add&&<Modal onClose={()=>setAdd(false)}><div className="modal-head"><div><h2>Add a moment</h2><p>It will appear on eligible guests’ schedules.</p></div><button className="icon-btn" onClick={()=>setAdd(false)}><X/></button></div><div className="form-grid"><label className="full">Title<input id="moment-title" placeholder="e.g. Welcome dinner"/></label><label>Time<input id="moment-time" type="time" defaultValue="18:00"/></label><label>Audience<select id="moment-audience"><option>All guests</option><option>Family & VIP</option><option>Friends</option></select></label><label className="full">Venue<input id="moment-place" placeholder="Venue name and address"/></label></div><div className="modal-actions"><button className="btn ghost" onClick={()=>setAdd(false)}>Cancel</button><button className="btn primary" onClick={()=>{const title=(document.getElementById('moment-title') as HTMLInputElement).value||'New event moment';const time=(document.getElementById('moment-time') as HTMLInputElement).value;setSchedule([...schedule,{id:Date.now(),title,time,place:(document.getElementById('moment-place') as HTMLInputElement).value||'Venue to be confirmed',audience:'All guests'}]);setAdd(false);notify('Schedule updated')}}>Add to schedule</button></div></Modal>}</AnimatePresence></div>}

function Seating({notify}:{notify:(s:string)=>void}){const {guests,seating,eventId,refresh}=useEventStore();const [selected,setSelected]=useState<Guest|null>(null);const [add,setAdd]=useState(false);const assign=async(table:string)=>{if(!selected){notify('Select an unseated guest first');return}try{await api.assignSeat(eventId,selected.id,table);setSelected(null);await refresh();notify(`Assigned ${selected.name} to ${table}`)}catch(e){notify(e instanceof Error?e.message:'Could not assign guest')}};return <div className="page"><PageIntro title="Seating planner" text="Select a guest, then choose their table." actions={<><button className="btn ghost" onClick={()=>window.print()}><Download size={16}/>Print floor plan</button><button className="btn primary" onClick={()=>setAdd(true)}><Plus size={16}/>Add table</button></>}/><div className="seating-layout"><aside className="card unseated"><h3>Unseated guests <span>{guests.filter(g=>!g.table&&g.status==='Attending').length}</span></h3><div className="searchbox"><Search size={15}/><input placeholder="Find guest"/></div>{guests.filter(g=>!g.table&&g.status==='Attending').map(g=><button className={'drag-guest '+(selected?.id===g.id?'selected':'')} key={g.id} onClick={()=>setSelected(g)}><GripVertical size={15}/><Avatar initials={g.initials}/><span><b>{g.name}</b><small>Party of {g.party}</small></span></button>)}{!guests.some(g=>!g.table&&g.status==='Attending')&&<div className="empty-note"><Users/><p>Every attending guest has a table.</p></div>}</aside><section className="floor"><div className="floor-tools"><span>Reception floor plan</span><small>{selected?`Now seat ${selected.name}`:'Select a guest to begin'}</small></div>{seating.map((t,i)=><button onClick={()=>assign(t.name)} className={'round-table t'+(i%5)} style={{left:`${Math.min(80,Math.max(8,t.x))}%`,top:`${Math.min(75,Math.max(12,t.y))}%`}} key={t.id}><div><b>{t.name}</b><small>{t.shape}</small><span>{t.assigned}/{t.capacity}</span></div>{Array.from({length:Math.min(Number(t.assigned),12)}).map((_,n)=><i key={n} style={{transform:`rotate(${n*(360/Math.max(1,Number(t.assigned)))}deg) translateY(-49px)`}}/>)}</button>)}{!seating.length&&<div className="floor-empty"><Armchair/><b>No tables yet</b><span>Add your first table to begin seating guests.</span></div>}<div className="dancefloor">DANCE FLOOR</div></section></div><AnimatePresence>{add&&<Modal onClose={()=>setAdd(false)}><div className="modal-head"><div><span className="eyebrow">FLOOR PLAN</span><h2>Add a table</h2></div><button className="icon-btn" onClick={()=>setAdd(false)}><X/></button></div><form id="table-form" className="form-grid" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await api.addTable(eventId,{name:f.get('name'),shape:f.get('shape'),capacity:Number(f.get('capacity')),x:20+seating.length%3*28,y:22+Math.floor(seating.length/3)*34});setAdd(false);await refresh();notify('Table added to the floor plan')}catch(err){notify(err instanceof Error?err.message:'Could not add table')}}}><label>Table name<input name="name" required placeholder="T01"/></label><label>Capacity<input name="capacity" type="number" min="1" max="100" defaultValue="10" required/></label><label className="full">Shape<select name="shape"><option value="round">Round</option><option value="rectangle">Rectangle</option><option value="head">Head table</option></select></label></form><div className="modal-actions"><button className="btn ghost" onClick={()=>setAdd(false)}>Cancel</button><button className="btn primary" type="submit" form="table-form">Add table</button></div></Modal>}</AnimatePresence></div>}

function Messages({notify}:{notify:(s:string)=>void}){const {eventId,event,guests}=useEventStore();const pendingCount=guests.filter(g=>g.status==='Pending').reduce((n,g)=>n+g.party,0);const attendingCount=guests.filter(g=>g.status==='Attending').reduce((n,g)=>n+g.party,0);const [sending,setSending]=useState(false);const [channel,setChannel]=useState('WhatsApp');const [text,setText]=useState(`Hello {{first_name}}, ${event?.title||'our celebration'} is almost here! Please confirm your attendance and view the latest details below.`);return <div className="page"><PageIntro title="Guest communications" text="Personal, timely updates across every channel." actions={<button className="btn ghost" onClick={()=>setText('Hello {{first_name}}, your event is tomorrow. Here are the latest times, venue details and your personal pass. We look forward to welcoming you.')}><Clock3 size={16}/>Use day-before automation</button>}/><div className="message-layout"><section className="card composer"><h3>Create announcement</h3><p>Send an update to a guest segment</p><label>Channel<div className="channel-tabs">{[['WhatsApp',MessageCircle],['Email',Mail],['SMS',Phone]].map(([x,I]:any)=><button key={x} className={channel===x?'active':''} onClick={()=>setChannel(x)}><I size={16}/>{x}</button>)}</div></label><label>Recipients<select><option>Guests awaiting reply · {pendingCount}</option><option>All invited guests · {guests.reduce((n,g)=>n+g.party,0)}</option><option>Confirmed guests · {attendingCount}</option><option>VIP guests · {guests.filter(g=>g.group==='VIP').length}</option></select></label><label>Message<textarea rows={7} value={text} onChange={e=>setText(e.target.value)}/><small>{text.length}/500 characters</small></label><div className="suggestions"><button onClick={()=>setText('Hello {{first_name}}, a gentle reminder to RSVP for our celebration by 1 November. We hope you can join us!')}>RSVP reminder</button><button onClick={()=>setText('Hello {{first_name}}, an important venue update for Amaka & Chidi’s wedding. Please view the latest details below.')}>Venue update</button><button onClick={()=>setText('Hello {{first_name}}, our celebration is tomorrow. Your schedule, directions and personal pass are ready at the link below.')}>Day-before note</button></div><button className="btn primary send-full" disabled={sending||!eventId} onClick={async()=>{setSending(true);try{await api.announce(eventId,{channel:channel.toLowerCase(),audience:'pending',message:text});notify(`${channel} announcement queued for ${pendingCount} guests`)}catch(e){notify(e instanceof Error?e.message:'Message could not be queued')}finally{setSending(false)}}}><Send size={16}/>{sending?'Queueing…':'Review & send'}</button></section><aside className="phone-preview"><div className="phone"><div className="phone-notch"/><div className="wa-head"><Avatar initials="A&C"/><span><b>Amaka & Chidi</b><small>online</small></span></div><div className="wa-body"><div className="wa-card"><img src="/wedding-hero.jpg"/><span>AMAKA & CHIDI</span><p>{text.replace('{{first_name}}','Kemi')}</p><small>2:34 PM ✓✓</small></div></div></div><p>Personalization preview for <b>Kemi</b></p></aside></div></div>}

function Checkin({notify}:{notify:(s:string)=>void}){const {guests,setGuests,eventId,refresh}=useEventStore();const [scanner,setScanner]=useState(false);const [q,setQ]=useState('');const shown=guests.filter(g=>g.status==='Attending'&&g.name.toLowerCase().includes(q.toLowerCase()));const checked=guests.filter(g=>g.checkedIn).reduce((a,g)=>a+g.party,0);const expected=guests.filter(g=>g.status==='Attending').reduce((a,g)=>a+g.party,0);return <div className="page"><PageIntro title="Event check-in" text="Fast, calm arrivals — even when the venue goes offline." actions={<><span className="offline-ready"><CloudSun size={16}/>Offline ready</span><button className="btn primary" onClick={()=>setScanner(true)}><ScanLine size={16}/>Scan QR pass</button></>}/><div className="checkin-metrics"><div><span>Checked in</span><b>{checked}</b><small>of {expected} expected</small></div><div><span>Arrival rate</span><b>{expected?Math.round(checked/expected*100):0}%</b><small>Peak at 4:42 PM</small></div><div><span>Stations online</span><b>3</b><small>Grand entrance · VIP · Garden</small></div></div><section className="card table-card"><div className="table-toolbar"><div className="searchbox grow"><Search size={16}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search guest by name, phone or table…"/></div></div><div className="check-list">{shown.map(g=><div key={g.id}><Avatar initials={g.initials}/><span><b>{g.name}</b><small>Party of {g.party} · {g.table||'No table'}</small></span><button className={g.checkedIn?'checked':''} onClick={async()=>{const next=!g.checkedIn;setGuests(guests.map(x=>x.id===g.id?{...x,checkedIn:next}:x));try{if(eventId)await api.checkin(eventId,g.id,next);notify(next?`${g.name} checked in`:'Check-in reversed')}catch(e){setGuests(guests);notify(e instanceof Error?e.message:'Check-in failed')}}}>{g.checkedIn?<><Check size={15}/>Checked in</>:<><TicketCheck size={15}/>Check in</>}</button></div>)}</div></section><AnimatePresence>{scanner&&<Modal onClose={()=>setScanner(false)}><div className="modal-head"><div><span className="eyebrow">SECURE EVENT ENTRY</span><h2>Scan a guest pass</h2><p>Paste the pass code from a QR scan, or use the name search behind this dialog.</p></div><button className="icon-btn" onClick={()=>setScanner(false)}><X/></button></div><form id="scan-form" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{const result=await api.scanPass(eventId,String(f.get('token')));await refresh();setScanner(false);notify(result.alreadyCheckedIn?`${result.guest.name} was already checked in`:`${result.guest.name} checked in successfully`)}catch(err){notify(err instanceof Error?err.message:'Pass could not be scanned')}}}><label className="scan-field">Invitation pass code<input name="token" required minLength={20} autoFocus placeholder="Scan or enter secure pass token"/></label></form><div className="scan-help"><QrCode/><span><b>Camera QR support</b><small>Use your device’s camera to copy the QR pass code into this secure field.</small></span></div><div className="modal-actions"><button className="btn ghost" onClick={()=>setScanner(false)}>Cancel</button><button className="btn primary" type="submit" form="scan-form"><ScanLine size={15}/>Validate pass</button></div></Modal>}</AnimatePresence></div>}

function Memories({notify}:{notify:(s:string)=>void}){const {eventId,event,media,refresh}=useEventStore();const [uploading,setUploading]=useState(false);const [galleryFilter,setGalleryFilter]=useState<'All'|'Pending'|'Approved'>('All');const shown=media.filter(item=>galleryFilter==='All'||item.status===galleryFilter.toLowerCase());return <div className="page"><PageIntro title="Memory vault" text="Collect the moments your photographer couldn’t be everywhere for." actions={<><button className="btn ghost" onClick={async()=>{await navigator.clipboard.writeText(`${location.origin}/invite/${event?.slug||eventId}#gallery`);notify('Guest upload link copied')}}><Copy size={16}/>Copy upload link</button><label className="btn primary upload-btn"><Upload size={16}/>{uploading?'Uploading…':'Add media'}<input type="file" accept="image/jpeg,image/png,image/webp,video/mp4,audio/mpeg" disabled={uploading} onChange={async e=>{const file=e.target.files?.[0];if(!file)return;setUploading(true);try{await api.uploadMedia(eventId,file);await refresh();notify('Media uploaded for review')}catch(err){notify(err instanceof Error?err.message:'Upload failed')}finally{setUploading(false);e.target.value=''}}}/></label></>}/><div className="memory-hero card"><div><span className="eyebrow">LIVE PHOTO WALL</span><h2>Every perspective.<br/><i>One beautiful story.</i></h2><p>Guests can contribute without an account. You decide what appears publicly.</p><div><span><b>{media.length}</b> uploads</span><span><b>{media.filter(x=>x.status==='approved').length}</b> approved</span><span><b>{media.filter(x=>x.status==='pending').length}</b> pending</span></div></div><img src="/gallery-dance.jpg" alt="Guests celebrating"/></div><div className="gallery-head"><h3>Moderation queue</h3><div className="filter-tabs">{(['All','Pending','Approved'] as const).map(f=><button key={f} className={galleryFilter===f?'active':''} onClick={()=>setGalleryFilter(f)}>{f==='All'?'All uploads':f}</button>)}</div></div>{shown.length?<div className="gallery-grid">{shown.map(item=><div className="gallery-item" key={item.id}>{item.mime_type.startsWith('image/')?<img src={api.mediaUrl(eventId,item.id)} alt={item.caption||'Guest-contributed event memory'}/>:<div className="media-file"><Images/><b>{item.mime_type.startsWith('video/')?'Video':'Audio'} memory</b><small>{Math.round(item.size_bytes/1024)} KB</small></div>}<div><span><Avatar initials="GM"/><b>Guest memory</b></span><div>{item.status==='pending'&&<button onClick={async()=>{await api.moderateMedia(eventId,item.id,'approved');await refresh();notify('Memory approved')}}><Check size={15}/>Approve</button>}<a className="icon-btn" href={api.mediaUrl(eventId,item.id)} download><Download size={15}/></a></div></div><em className={item.status}>{item.status==='pending'?'Needs review':item.status}</em></div>)}</div>:<EmptyState icon={Images} title={galleryFilter==='All'?'Your memory vault is ready':'Nothing in this queue'} text={galleryFilter==='All'?'Upload the first photograph, then share the guest contribution link.':'Choose another filter to view collected memories.'} action="Add media" onAction={()=>document.querySelector<HTMLInputElement>('.upload-btn input')?.click()}/>}</div>}

function Budget({notify}:{notify:(s:string)=>void}){const {budgets,eventId,refresh}=useEventStore();const [open,setOpen]=useState(false);const rows=budgets.map((b,i)=>({id:b.id,name:b.category,spent:b.spent_minor/100,budget:b.budget_minor/100,color:['#6f7565','#a67c52','#c0a77b','#919986','#b98174'][i%5]}));const spent=rows.reduce((a,b)=>a+b.spent,0),total=rows.reduce((a,b)=>a+b.budget,0);return <div className="page"><PageIntro title="Budget" text="A clear view of every naira, commitment and remaining balance." actions={<button className="btn primary" onClick={()=>setOpen(true)}><Plus size={16}/>Add category</button>}/>{rows.length?<><div className="budget-hero"><div className="card"><span>Total budget</span><b>{fmt(total)}</b><div className="budget-bar"><i style={{width:`${total?Math.min(100,spent/total*100):0}%`}}/></div><p><strong>{fmt(spent)}</strong> committed <em>{fmt(Math.max(0,total-spent))} remaining</em></p></div><div className="card"><span>Budget health</span><b>{total?Math.round(spent/total*100):0}%</b><p>Committed across {rows.length} categories</p></div></div><section className="card categories"><CardHead title="Spending by category" sub="Committed against planned budget"/><div>{rows.map(b=><div className="category-row" key={b.id}><i style={{background:b.color}}/><span><b>{b.name}</b><small>{fmt(b.spent)} of {fmt(b.budget)}</small></span><div><i style={{width:`${b.budget?Math.min(100,b.spent/b.budget*100):0}%`,background:b.color}}/></div><strong>{b.budget?Math.round(b.spent/b.budget*100):0}%</strong><button className="plain" aria-label={`Delete ${b.name}`} onClick={async()=>{await api.deleteBudget(eventId,b.id);await refresh();notify('Budget category removed')}}><Trash2 size={15}/></button></div>)}</div></section></>:<EmptyState icon={WalletCards} title="Build a calm, clear budget" text="Add your first category to track planned and committed costs." action="Add category" onAction={()=>setOpen(true)}/>}<AnimatePresence>{open&&<Modal onClose={()=>setOpen(false)}><div className="modal-head"><div><span className="eyebrow">NEW BUDGET CATEGORY</span><h2>Plan an expense</h2></div><button className="icon-btn" onClick={()=>setOpen(false)}><X/></button></div><form id="budget-form" className="form-grid" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);await api.addBudget(eventId,{category:f.get('category'),budget:Number(f.get('budget')),spent:Number(f.get('spent')||0)});setOpen(false);await refresh();notify('Budget category added')}}><label className="full">Category<input name="category" required placeholder="e.g. Venue & catering"/></label><label>Planned amount (₦)<input name="budget" type="number" min="0" required/></label><label>Already committed (₦)<input name="spent" type="number" min="0" defaultValue="0"/></label></form><div className="modal-actions"><button className="btn ghost" onClick={()=>setOpen(false)}>Cancel</button><button className="btn primary" type="submit" form="budget-form">Save category</button></div></Modal>}</AnimatePresence></div>}
function Vendors({notify}:{notify:(s:string)=>void}){const {vendors,eventId,refresh}=useEventStore();const [open,setOpen]=useState(false);return <div className="page"><PageIntro title="Vendor workspace" text="Contracts, contacts, deliverables and payments — without the spreadsheet chaos." actions={<button className="btn primary" onClick={()=>setOpen(true)}><Plus size={16}/>Add vendor</button>}/>{vendors.length?<div className="vendor-grid">{vendors.map(v=><div className="card vendor" key={v.id}><div><Avatar initials={v.name.split(' ').slice(0,2).map(x=>x[0]).join('').toUpperCase()}/><button className="plain" aria-label={`Delete ${v.name}`} onClick={async()=>{await api.deleteVendor(eventId,v.id);await refresh();notify('Vendor removed')}}><Trash2 size={16}/></button></div><h3>{v.name}</h3><p>{v.category}</p><div className="vendor-meta"><span className={'pay '+v.payment_status.replace('_','-')}>{v.payment_status.replace('_',' ')}</span><b>{fmt(v.contract_amount_minor/100)}</b></div><div className="vendor-actions">{v.email?<a href={`mailto:${v.email}`}><Mail size={15}/>Email</a>:<span>No email added</span>}</div></div>)}</div>:<EmptyState icon={Handshake} title="Bring your team together" text="Add planners, caterers, photographers and every partner behind the day." action="Add vendor" onAction={()=>setOpen(true)}/>}<AnimatePresence>{open&&<Modal onClose={()=>setOpen(false)}><div className="modal-head"><div><span className="eyebrow">NEW PARTNER</span><h2>Add a vendor</h2></div><button className="icon-btn" onClick={()=>setOpen(false)}><X/></button></div><form id="vendor-form" className="form-grid" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);await api.addVendor(eventId,{name:f.get('name'),category:f.get('category'),email:f.get('email'),amount:Number(f.get('amount')||0),status:f.get('status')});setOpen(false);await refresh();notify('Vendor added')}}><label className="full">Business or vendor name<input name="name" required/></label><label>Category<input name="category" required placeholder="Photography"/></label><label>Contract amount (₦)<input name="amount" type="number" min="0"/></label><label>Email<input name="email" type="email"/></label><label>Payment status<select name="status"><option value="due">Due</option><option value="part_paid">Part paid</option><option value="paid">Paid</option></select></label></form><div className="modal-actions"><button className="btn ghost" onClick={()=>setOpen(false)}>Cancel</button><button className="btn primary" type="submit" form="vendor-form">Save vendor</button></div></Modal>}</AnimatePresence></div>}
function Analytics(){const {analytics,guests}=useEventStore();const views=analytics.view||0;const responded=guests.filter(g=>g.status!=='Pending').length;const conversion=guests.length?Math.round(responded/guests.length*100):0;const events=[['Invitation views',views,'view'],['Section interactions',analytics.section_view||0,'section_view'],['Shares',analytics.share||0,'share'],['Calendar saves',analytics.calendar||0,'calendar'],['Direction opens',analytics.direction||0,'direction']] as const;const max=Math.max(1,...events.map(x=>Number(x[1])));return <div className="page"><PageIntro title="Analytics" text="Understand every view, response and guest interaction." actions={<button className="btn ghost" onClick={()=>{const csv='Metric,Count\n'+events.map(x=>`${x[0]},${x[1]}`).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv]));a.download='event-analytics.csv';a.click()}}><Download size={16}/>Export report</button>}/><section className="metric-grid analytics"><Metric title="Invitation views" value={String(views)} detail="Privacy-safe event views" icon={Eye} tone="sage"/><Metric title="RSVP completion" value={`${conversion}%`} detail={`${responded} of ${guests.length} guest records`} icon={TrendingUp} tone="green"/><Metric title="Shares" value={String(analytics.share||0)} detail="Tracked invitation shares" icon={Share2} tone="amber"/><Metric title="Direction opens" value={String(analytics.direction||0)} detail="Guests checking the venue" icon={Navigation} tone="rose"/></section><div className="analytics-grid"><section className="card engagement-bars"><CardHead title="Engagement signals" sub="First-party, privacy-conscious interactions"/><div>{events.map(([label,count])=><span key={label}><b>{label}</b><i><em style={{width:`${Number(count)/max*100}%`}}/></i><strong>{count}</strong></span>)}</div></section><section className="card response-ring"><span style={{'--response':`${conversion*3.6}deg`} as React.CSSProperties}><b>{conversion}%</b><small>responded</small></span><h3>Guest response health</h3><p>{guests.length?`${guests.length-responded} guest records still need a response.`:'Add guests to begin measuring response health.'}</p></section></div></div>}

function SettingsPage({notify}:{notify:(s:string)=>void}){const {event,eventId,refresh}=useEventStore();const initialCapabilities=(()=>{try{return JSON.parse(event?.settings_json||'{}').capabilities||['rsvp','seating','accommodation','gifts','memories']}catch{return ['rsvp','seating','memories']}})();const [capabilities,setCapabilities]=useState<string[]>(initialCapabilities);const save=async()=>{const title=(document.getElementById('setting-title') as HTMLInputElement).value;const eventType=(document.getElementById('setting-type') as HTMLSelectElement).value;const lifecycle=(document.getElementById('setting-lifecycle') as HTMLSelectElement).value;const date=(document.getElementById('setting-date') as HTMLInputElement).value;const locationValue=(document.getElementById('setting-location') as HTMLInputElement).value;try{await api.updateEvent(eventId,{title,eventType,lifecycle,startsAt:`${date}T12:00:00+01:00`,location:locationValue,settings:{capabilities}});await refresh();notify('Event settings saved')}catch(e){notify(e instanceof Error?e.message:'Could not save settings')}};return <div className="page settings-page"><PageIntro title="Event settings" text="Identity, access, localization and event capabilities." actions={<button className="btn primary" onClick={save}><Save size={16}/>Save changes</button>}/><div className="settings-layout single"><section className="card settings-form"><div><h3>Event identity</h3><p>The core information used throughout your invitation.</p></div><div className="form-grid"><label className="full">Event title<input id="setting-title" defaultValue={event?.title||''}/></label><label>Event type<select id="setting-type" defaultValue={event?.event_type||'wedding'}><option value="wedding">Wedding</option><option value="birthday">Birthday</option><option value="conference">Conference</option><option value="memorial">Funeral / memorial</option><option value="custom">Custom</option></select></label><label>Lifecycle state<select id="setting-lifecycle" defaultValue={event?.lifecycle||'draft'}><option value="published">Published</option><option value="draft">Draft</option><option value="live">Live</option><option value="completed">Completed</option></select></label><label>Date<input id="setting-date" type="date" defaultValue={event?.starts_at?.slice(0,10)||''}/></label><label>Time zone<select><option>Africa/Lagos · WAT</option></select></label><label className="full">Primary location<input id="setting-location" defaultValue={event?.location||''}/></label></div><hr/><div><h3>Event capabilities</h3><p>Turn features on as you need them. Guests only see what applies to them.</p></div><div className="capabilities">{[[CalendarDays,'Multiple occasions','Traditional, white wedding and reception','occasions'],[Users,'Guest RSVP','Personalized responses and plus-ones','rsvp'],[Armchair,'Seating planner','Tables and guest assignments','seating'],[Bus,'Transportation','Pickup points and seat requests','transport'],[Hotel,'Accommodation','Hotels and room blocks','accommodation'],[Gift,'Registry & gifting','Gift links and cash contributions','gifts'],[Images,'Memory vault','Guest uploads and moderation','memories'],[Bot,'AI event assistant','Copy, schedules and guest answers','ai']].map(([I,n,d,id]:any)=><div key={n}><span><I size={18}/><b>{n}</b><small>{d}</small></span><button aria-label={`Toggle ${n}`} onClick={()=>setCapabilities(capabilities.includes(id)?capabilities.filter(x=>x!==id):[...capabilities,id])} className={'toggle '+(capabilities.includes(id)?'on':'')}><i/></button></div>)}</div><hr/><div><h3>Visibility & privacy</h3><p>Protect sensitive event and guest information.</p></div><div className="privacy-box"><ShieldCheck/><span><b>Personalized invitation links</b><p>Private occasions and guest details require signed, guest-specific access.</p></span><span className="secure-label"><Lock size={13}/>Enabled</span></div></section></div></div>}

function EmptyState({icon:Icon,title,text,action,onAction}:{icon:any,title:string,text:string,action:string,onAction:()=>void}){return <div className="empty-state card"><span><Icon/></span><h3>{title}</h3><p>{text}</p><button className="btn primary" onClick={onAction}><Plus size={15}/>{action}</button></div>}

function PageIntro({title,text,actions}:{title:string,text:string,actions?:React.ReactNode}){return <div className="page-intro"><div><h2>{title}</h2><p>{text}</p></div><div>{actions}</div></div>}

function PublicInvite(){const [open,setOpen]=useState(false);const [submitted,setSubmitted]=useState(false);const [publicData,setPublicData]=useState<any>(null);const [publicError,setPublicError]=useState('');const [choices,setChoices]=useState<Record<string,'attending'|'declined'>>({});const [submitting,setSubmitting]=useState(false);const params=new URLSearchParams(location.search);const token=params.get('token')||localStorage.getItem('invibox.guestToken')||'';const slug=location.pathname.split('/').filter(Boolean).pop()||'amaka-chidi';useEffect(()=>{api.publicEvent(slug,token).then(data=>{setPublicData(data);setChoices(Object.fromEntries(data.occasions.map((o:any)=>[o.id,o.rsvp_status==='declined'?'declined':'attending'])));api.analytics({eventId:data.event.id,type:'view',session:sessionStorage.getItem('invibox.session')||crypto.randomUUID()}).catch(()=>{})}).catch(e=>setPublicError(e instanceof Error?e.message:'Invitation unavailable'))},[]);const occasions=publicData?.occasions||[];if(publicError)return <div className="invite-error"><span className="invite-brand">invibox</span><Heart/><h1>This invitation is resting.</h1><p>{publicError}. Please ask your host for the latest private link.</p><a href="/">Discover Invibox</a></div>;return <div className="public-invite"><nav><a className="invite-brand" href="/">invibox</a><div><button onClick={async()=>{if(navigator.share)await navigator.share({title:publicData?.event?.title||'You are invited',url:location.href});else await navigator.clipboard.writeText(location.href);if(publicData?.event?.id)api.analytics({eventId:publicData.event.id,type:'share'}).catch(()=>{})}}><Share2 size={15}/>Share</button><button onClick={()=>setOpen(true)}>RSVP</button></div></nav><section className="public-hero"><img src="/wedding-hero.jpg"/><div className="public-shade"/><motion.div initial={{opacity:0,y:28}} animate={{opacity:1,y:0}} transition={{duration:1}}><span>TOGETHER WITH OUR FAMILIES</span><h1>{publicData?.event?.title||<>Amaka <i>&</i><br/>Chidi</>}</h1><p>invite you to share in the joy<br/>of their wedding celebration</p><strong>SATURDAY · 19 DECEMBER · 2026</strong><button onClick={()=>document.getElementById('details')?.scrollIntoView({behavior:'smooth'})}>DISCOVER OUR DAY <ChevronDown size={16}/></button></motion.div></section><section className="invite-quote" id="details"><span>“</span><h2>Two families. One promise.<br/><i>A lifetime begins.</i></h2><p>From a chance meeting in Lagos to this beautiful moment,<br/>we cannot imagine celebrating without you.</p></section><section className="invite-details"><div><span>01</span><Clock3/><h3>Traditional ceremony</h3><p>10 o’clock in the morning</p><b>The Monarch Hall, Lekki</b><button onClick={()=>{if(publicData?.event?.id)api.analytics({eventId:publicData.event.id,type:'direction'}).catch(()=>{});window.open('https://www.google.com/maps/search/?api=1&query='+encodeURIComponent('The Monarch Hall, Lekki, Lagos'),'_blank','noopener')}}><Navigation size={14}/>Directions</button></div><div className="details-photo"><img src="/gallery-couple.jpg"/></div><div><span>02</span><Heart/><h3>White wedding</h3><p>Half past one in the afternoon</p><b>Grace Pavilion, Victoria Island</b><button onClick={()=>{if(publicData?.event?.id)api.analytics({eventId:publicData.event.id,type:'calendar'}).catch(()=>{});const ics='BEGIN:VCALENDAR\r\nVERSION:2.0\r\nBEGIN:VEVENT\r\nDTSTART:20261219T123000Z\r\nDTEND:20261219T150000Z\r\nSUMMARY:Amaka & Chidi — White Wedding\r\nLOCATION:Grace Pavilion, Victoria Island, Lagos\r\nEND:VEVENT\r\nEND:VCALENDAR';const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([ics],{type:'text/calendar'}));a.download='amaka-chidi-wedding.ics';a.click();URL.revokeObjectURL(a.href)}}><CalendarPlus size={14}/>Add to calendar</button></div></section><section className="invite-gallery"><span>OUR STORY, IN MOMENTS</span><h2>Made of little things</h2><div><img src="/gallery-dance.jpg"/><img src="/gallery-table.jpg"/><img src="/gallery-couple.jpg"/></div></section><section className="invite-rsvp"><span>THE PLEASURE OF YOUR COMPANY</span><h2>Will you join us?</h2><p>Kindly respond by the first of November.</p><button onClick={()=>setOpen(true)}>RSVP NOW <ArrowUpRight size={15}/></button></section><footer><b>{publicData?.event?.title||<>Amaka <i>&</i> Chidi</>}</b><span>#AnchoredInLove2026</span><small>Made beautifully with invibox</small></footer><AnimatePresence>{open&&<Modal onClose={()=>setOpen(false)}>{submitted?<div className="rsvp-success"><span><Check/></span><h2>Your reply is saved</h2><p>Thank you, {publicData?.guest?.name||'dear guest'}. We can’t wait to celebrate with you.</p><button className="btn primary" onClick={()=>setOpen(false)}>Return to invitation</button></div>:<div className="rsvp-modal"><div className="modal-head"><div><span className="eyebrow">PERSONAL INVITATION</span><h2>Dear {publicData?.guest?.name||'Guest'},</h2><p>Please let us know which moments you’ll attend.</p></div><button className="icon-btn" onClick={()=>setOpen(false)}><X/></button></div>{(occasions.length?occasions:[{id:'preview-1',title:'Traditional ceremony',starts_at:'2026-12-19T10:00:00+01:00'},{id:'preview-2',title:'White wedding',starts_at:'2026-12-19T13:30:00+01:00'},{id:'preview-3',title:'Reception & dinner',starts_at:'2026-12-19T17:00:00+01:00'}]).map((x:any)=><div className="rsvp-event" key={x.id}><span><b>{x.title}</b><small>{new Date(x.starts_at).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})}</small></span><div><button className={choices[x.id]==='attending'?'selected':''} onClick={()=>setChoices({...choices,[x.id]:'attending'})}><Check size={14}/>Joyfully accepts</button><button className={choices[x.id]==='declined'?'selected decline':''} onClick={()=>setChoices({...choices,[x.id]:'declined'})}><X size={14}/>Declines</button></div></div>)}<label>Dietary notes<textarea id="dietary-notes" placeholder="Allergies or dietary requirements…"/></label><button className="btn primary send-full" disabled={submitting||!token||!occasions.length} onClick={async()=>{setSubmitting(true);try{await api.rsvp({token,responses:occasions.map((o:any)=>({occasionId:o.id,status:choices[o.id]||'attending'})),dietaryNotes:(document.getElementById('dietary-notes') as HTMLTextAreaElement)?.value||''});setSubmitted(true)}catch(e){alert(e instanceof Error?e.message:'Unable to save your response')}finally{setSubmitting(false)}}}>{submitting?'Saving…':'Confirm my response'} <ArrowUpRight size={15}/></button></div>}</Modal>}</AnimatePresence></div>}

function NewEvent({onClose,notify,onCreate}:{onClose:()=>void,notify:(s:string)=>void,onCreate:(data:{title:string,eventType:string,date:string,location:string})=>Promise<void>}){const [step,setStep]=useState(0);const [eventType,setEventType]=useState('Wedding');const [creating,setCreating]=useState(false);const types=[['Wedding',Heart],['Birthday',Sparkles],['Conference',Users],['Memorial',BookHeart],['Celebration',Gift],['Custom',Plus]];return <Modal onClose={onClose} wide><div className="modal-head"><div><span className="eyebrow">CREATE AN EXPERIENCE</span><h2>{step===0?'What are you celebrating?':'Give your event a name'}</h2><p>{step===0?'We’ll tailor the flow, wording and capabilities.':'You can change every detail later.'}</p></div><button className="icon-btn" onClick={onClose}><X/></button></div>{step===0?<div className="type-grid">{types.map(([n,I]:any)=><button key={n} onClick={()=>{setEventType(n);setStep(1)}}><I/><b>{n}</b><span>Start with a smart event pack</span></button>)}</div>:<><div className="form-grid"><label className="full">Event name<input id="new-event-title" autoFocus placeholder="e.g. Ada & David"/></label><label>Date<input id="new-event-date" type="date" defaultValue="2026-12-19"/></label><label>Location<input id="new-event-location" placeholder="City, country" defaultValue="Lagos, Nigeria"/></label></div><div className="modal-actions"><button className="btn ghost" onClick={()=>setStep(0)}>Back</button><button className="btn primary" disabled={creating} onClick={async()=>{const title=(document.getElementById('new-event-title') as HTMLInputElement).value.trim();const date=(document.getElementById('new-event-date') as HTMLInputElement).value;const locationValue=(document.getElementById('new-event-location') as HTMLInputElement).value.trim();if(!title||!date||!locationValue){notify('Complete the event name, date and location');return}setCreating(true);try{await onCreate({title,eventType:eventType.toLowerCase(),date,location:locationValue});onClose();notify('New event workspace created')}catch(e){notify(e instanceof Error?e.message:'Event could not be created');setCreating(false)}}}>{creating?'Creating…':'Create event'} <ArrowUpRight size={15}/></button></div></>}</Modal>}

function AppShell(){
 if(location.pathname.startsWith('/invite/'))return <PublicInvite/>;
 const {event,createFirst}=useEventStore();
 const [page,setPage]=useState<PageKey>('overview');const [mobile,setMobile]=useState(false);const [newEvent,setNewEvent]=useState(false);const [toast,setToast]=useState('');
 const notify=(s:string)=>{setToast(s);window.setTimeout(()=>setToast(''),3000)};
 const content=useMemo(()=>({overview:<Overview setPage={setPage} notify={notify}/>,experience:<Experience notify={notify}/>,guests:<GuestsPage notify={notify}/>,schedule:<SchedulePage notify={notify}/>,seating:<Seating notify={notify}/>,messages:<Messages notify={notify}/>,checkin:<Checkin notify={notify}/>,memories:<Memories notify={notify}/>,budget:<Budget notify={notify}/>,vendors:<Vendors notify={notify}/>,analytics:<Analytics/>,settings:<SettingsPage notify={notify}/>}),[page]);
 return <div className="app"><Sidebar page={page} setPage={setPage} mobile={mobile} setMobile={setMobile}/>{mobile&&<div className="sidebar-scrim" onClick={()=>setMobile(false)}/>}<div className="main"><Topbar page={page} onMobile={()=>setMobile(true)} onNew={()=>setNewEvent(true)} onPreview={()=>window.open(`/invite/${event?.slug||'amaka-chidi'}`,'_blank')}/><AnimatePresence mode="wait"><motion.div key={page} initial={{opacity:0,y:6}} animate={{opacity:1,y:0}} exit={{opacity:0}} transition={{duration:.2}}>{content[page]}</motion.div></AnimatePresence></div><AnimatePresence>{toast&&<Toast text={toast}/>} {newEvent&&<NewEvent onClose={()=>setNewEvent(false)} notify={notify} onCreate={createFirst}/>}</AnimatePresence></div>
}
function AccessPortal(){const {phase,error,authenticate,createFirst}=useEventStore();const [mode,setMode]=useState<'login'|'register'>('register');const [busy,setBusy]=useState(false);if(phase==='loading')return <div className="boot-screen"><span className="brandmark">i</span><b>invibox</b><i/></div>;if(phase==='auth')return <div className="access-page"><div className="access-art"><img src="/wedding-hero.jpg"/><div><span>ONE PLATFORM · EVERY CELEBRATION</span><h1>Create moments<br/>people <i>remember.</i></h1><p>From the first invitation to the final memory.</p></div></div><main className="access-form"><div className="brand"><span className="brandmark">i</span><b>invibox</b></div><div className="access-copy"><span className="eyebrow">{mode==='register'?'BEGIN YOUR EXPERIENCE':'WELCOME BACK'}</span><h2>{mode==='register'?'Create your account':'Sign in to Invibox'}</h2><p>{mode==='register'?'Your extraordinary event starts here.':'Continue orchestrating your celebration.'}</p></div><form onSubmit={async e=>{e.preventDefault();setBusy(true);const form=new FormData(e.currentTarget);try{await authenticate(mode,{name:String(form.get('name')||''),email:String(form.get('email')),password:String(form.get('password'))})}catch{}finally{setBusy(false)}}}>{mode==='register'&&<label>Your name<input name="name" required minLength={2} autoComplete="name"/></label>}<label>Email address<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required minLength={10} autoComplete={mode==='register'?'new-password':'current-password'}/><small>At least 10 characters</small></label>{error&&<div className="form-error" role="alert">{error}</div>}<button className="btn primary" disabled={busy}>{busy?'Please wait…':mode==='register'?'Create account':'Sign in'}<ArrowUpRight size={15}/></button></form><p className="mode-switch">{mode==='register'?'Already have an account?':'New to Invibox?'} <button onClick={()=>setMode(mode==='register'?'login':'register')}>{mode==='register'?'Sign in':'Create account'}</button></p></main></div>;if(phase==='onboarding')return <FirstEvent onCreate={createFirst}/>;return <AppShell/>}
function FirstEvent({onCreate}:{onCreate:(data:{title:string,eventType:string,date:string,location:string})=>Promise<void>}){const [busy,setBusy]=useState(false);return <div className="first-event"><div className="brand"><span className="brandmark">i</span><b>invibox</b></div><main><span className="eyebrow">YOUR FIRST EXPERIENCE</span><h1>What are we<br/><i>celebrating?</i></h1><p>Start simply. Invibox will reveal the right tools as your event grows.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{await onCreate({title:String(f.get('title')),eventType:String(f.get('eventType')),date:String(f.get('date')),location:String(f.get('location'))})}finally{setBusy(false)}}}><label>Event name<input name="title" required minLength={2} placeholder="e.g. Ada & David"/></label><div><label>Event type<select name="eventType"><option value="wedding">Wedding</option><option value="birthday">Birthday</option><option value="conference">Conference</option><option value="memorial">Memorial</option><option value="custom">Custom event</option></select></label><label>Date<input name="date" required type="date"/></label></div><label>Location<input name="location" required placeholder="City, country"/></label><button className="btn primary" disabled={busy}>{busy?'Creating your workspace…':'Create my event'}<ArrowUpRight size={15}/></button></form></main></div>}
export default function App(){const path=location.pathname;if(path.startsWith('/invite/'))return <PublicInvite/>;if(path==='/app'||path.startsWith('/app/'))return <EventStoreProvider><AccessPortal/></EventStoreProvider>;return <Marketing/>}
